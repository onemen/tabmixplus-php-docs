      <div id="title" class="th">Mouse - Mouse Gestures</div>
      <div class="content-body evenbg">
        <div class="topic">
          <img src="help/images/TMPMouseMouseGestures.jpg" alt="image">
          <ul>
            <li>Select tab pointed for: (a tab may be selected and and in focus
            by pointing to the tab after the specified time)
              <ul>
                <li>Use time delay of (specify time 250 milliseconds is default)</li>
              </ul>
            </li>
            <li>Switch to last selected tab when clicking current one (clicking the
            current tab will make the last selected tab take focus and become the current
            tab after the specified time delay)
              <ul>
                <li>Use time delay of (specify time 250 milliseconds is default)</li>
              </ul>
            </li>
            <li>Mouse click (down and release) to select a tab</li>
            <li class="default">Remove Tab Mix Plus menu list entries using middle-click</li>
            <li class="default">When closing a tab with mouse click on close button
            on the tab, other tabs should not resize until cursor leaves toolbar region</li>
          </ul>
        </div>

        <div class="topic">
          <span class="default">When dragging a tab move it directly</span>,
          this feature enabled only when tabs are in single row.<br>
          Drop indicator (select one):
          <ul>
            <li class="default">Native Style (single down arrow)</li>
            <li>Built-in style (left and right arrows)</li>
          </ul>
        </div>

        <div class="topic">
          When scrolling over the tab-bar:
          <ul class="comment" style="margin-left: 0px;">
            <li>Hold Shift while scrolling to switch between these options</li>
            <li>Inverse scroll direction - change the direction in which tabs are selected or tab-bar is moved</li>
          </ul>
          <ul>
            <li>Scroll to change selected tab - scrolling down selects the next tab</li>
            <li><span class="default">Scroll all tabs</span> - scrolling move visible tabs on the tab-bar (when scroll buttons are visible)</li>
          </ul>
        </div>

      </div>
